// User roles
const userRoles = ["user", "admin"];

// Account status
const accountStatus = ["active", "suspended", "pending"];

// OTP types
const otpTypes = ["register", "reset-password", "verify-email"];

// Content status (blog posts, courses)
const contentStatus = ["draft", "published", "archived"];

// Booking (contact/reservation) lifecycle status
const bookingStatus = ["new", "contacted", "confirmed", "cancelled"];

export {
  userRoles,
  accountStatus,
  otpTypes,
  contentStatus,
  bookingStatus,
};
